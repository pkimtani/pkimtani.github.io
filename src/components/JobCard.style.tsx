import {Card, styled} from "@mui/material";
import {SECONDARY_BG_COLOR} from "../utils/contatns";

export const JobCardWrapper = styled(Card)`
  && {
    background-color: ${SECONDARY_BG_COLOR};
    border-radius: 2rem;
  }
`